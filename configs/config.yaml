defaults:
  - task: mnist
  - policy: cnn
  - solver: pgpe
  - trainer: trainer
  - hydra: base
  - wandb: disabled
  - run: mnist # this changes task, policy, solver, trainer.*
  - mode: train # this changes trainer.max_iter and wandb.mode
  - _self_

seed: 42
log_dir: ${trainer.log_dir}
gpu_id: 0