# @package _global_
defaults:
  - override /task: mnist
  - override /policy: cnn
  - override /solver: cem
  
solver:
  pop_size: 128
  elite_size: 32
  
trainer:
  max_iter: 5000
  log_interval: 100
  test_interval: 1000
  n_repeats: 1
  n_evaluations: 1